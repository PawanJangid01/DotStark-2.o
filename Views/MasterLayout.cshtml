@using Umbraco.Cms.Web.Common.PublishedModels;
@inherits Umbraco.Cms.Web.Common.Views.UmbracoViewPage
@using ContentModels = Umbraco.Cms.Web.Common.PublishedModels;
@{
		Layout = null;
}


<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>DotStark AI</title>
  <meta name="description"
    content="We help enterprises unlock business value through AI-driven platforms and Kentico-powered digital experiences." />
 <link rel="stylesheet" href="/css/styles.css" />
 <link rel="icon" href="/images/ds_favicon.jpg" type="image/png" /> 

  @RenderSection("Styles", required: false)
</head>

<body>

  <!-- â•â•â• HEADER â•â•â• -->
  <header class="header">
    <div class="container">
      <a href="/" class="logo"><img src="/images/dotstark_2.o.png" alt="DotStark Logo" style="height: 50px;" /></a>
      <nav>
        <a href="/rag">RAG</a>
        <a href="/ai-agents">AI Agents</a>
        <a href="/intelligent-automation">Intelligent Automation</a>
        <a href="/products">Products</a>    
        <a href="/insight">Insights</a>
        <a href="/contact-us">Contact Us</a>
      </nav>
      <a href="/demo" class="header-cta">View Demo</a>
      <button class="mobile-toggle" id="mobileToggle" aria-label="Open menu" aria-expanded="false">
        <svg id="iconMenu" width="24" height="24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round">
          <line x1="3" y1="6" x2="21" y2="6" />
          <line x1="3" y1="12" x2="21" y2="12" />
          <line x1="3" y1="18" x2="21" y2="18" />
        </svg>
        <svg id="iconClose" width="24" height="24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" style="display:none">
          <line x1="18" y1="6" x2="6" y2="18" />
          <line x1="6" y1="6" x2="18" y2="18" />
        </svg>
      </button>
  </header>

  <!-- Mobile nav drawer -->
  <nav class="mobile-nav" id="mobileNav">
    <a href="/rag">RAG</a>
    <a href="/ai-agents">AI Agents</a>
        <a href="/intelligent-automation">Intelligent Automation</a>
    <a href="/products">Products</a>
    <a href="/insight">Insights</a>
    <a href="/contact-us">Contact Us</a>
    <a href="/demo" class="mobile-cta">View Demo</a>
  </nav>

         <!-- ✅ MAIN CONTENT -->
        <main class="main">
            @RenderBody()
        </main>

 <!-- Cookie Consent -->
<div id="cookieBanner" class="cookie-banner">
  <div class="cookie-content">
    <p>
      We use cookies to enhance your experience. By continuing, you agree to our
      <a href="/privacy-policy">Privacy Policy</a>.
    </p>

    <div class="cookie-actions">
      <button id="cookieDecline" class="btn-outline">Decline</button>
      <button id="cookieAccept" class="btn-primary">Allow All Cookies</button>
    </div>
  </div>
</div>

  <!-- â•â•â• 11. FOOTER â•â•â• -->
  <footer class="footer section-padding">

    <div class="container">
      <div class="footer-grid">
        <div class="brand">
          <img src="/images/dotstark_2.o_footer.png" alt="DotStark Logo" style="height: 24px;" />
                    <p>AI Systems for Revenue, Automation and Decision Intelligence</p>
        </div>
        @* <div class="footer-col">
          <h4>Services</h4>
          <ul>
            <li><a href="#">Artificial Intelligence</a></li>
            <li><a href="#">Kentico Development</a></li>
          </ul>
        </div> *@
        <div class="footer-col">
          <h4>Solutions</h4>
          <ul>
            <li><a href="/rag">RAG</a></li>
            <li><a href="/ai-agent">AI Agents</a></li>
                        <li><a href="/intelligent-automation">Intelligent Automation</a></li>
          </ul>
        </div>
        <div class="footer-col">
          <h4>Company</h4>
          <ul>
            <li><a href="/about-us">About</a></li>
            <li><a href="/products">Products</a></li>
            <li><a href="/insight">Insights</a></li>
          </ul>
        </div>
        <div class="footer-col">
          <h4>Contact</h4>
          <ul>
            <li><a href="mailto:hello@dotstark.com" target="_blank" rel="noopener noreferrer">hr@dotstark.com</a></li>
            @* <li><a href="https://www.linkedin.com/company/dotstark-technologies/posts/?feedView=all" target="_blank" rel="noopener noreferrer">LinkedIn</a></li> *@
          </ul>
        </div>
      </div>
      <div class="footer-bottom">
        <p style="font-size: 14px;">&copy; 2026 DotStark. All Rights Reserved.</p>
<div class="footer-links privacy-box">
  <a href="/privacy-policy" class="privacy-link">
    
    <!-- SVG Icon -->
    <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" 
         viewBox="0 0 24 24" fill="none" stroke="currentColor" 
         stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
      <path d="M12 2l7 4v6c0 5-3.5 9-7 10-3.5-1-7-5-7-10V6l7-4z"></path>
      <path d="M9 12l2 2 4-4"></path>
    </svg>

    <!-- Text -->
    <span>We respect your <strong>privacy</strong></span>
  </a>
</div>
      </div>
    </div>
  </footer>

    <script>
                        const mobileToggle = document.getElementById("mobileToggle");
        const mobileNav = document.getElementById("mobileNav");
        const iconMenu = document.getElementById("iconMenu");
        const iconClose = document.getElementById("iconClose");

        if (mobileToggle && mobileNav) {
          mobileToggle.addEventListener("click", () => {

            mobileNav.classList.toggle("open");

            const isOpen = mobileNav.classList.contains("open");
            mobileToggle.setAttribute("aria-expanded", isOpen);

            if (iconMenu && iconClose) {
              iconMenu.style.display = isOpen ? "none" : "block";
              iconClose.style.display = isOpen ? "block" : "none";
            }
          });
        }
        // ── Testimonial slider ──
        const slides = document.querySelectorAll('.testi-slide');
        let current = 0;
        let autoTimer;
        let typingInterval;

        function typeText(slide) {
          if (!slide) return;

          clearInterval(typingInterval);

          const blockquote = slide.querySelector("blockquote");
          if (!blockquote) return;

          let fullText = blockquote.getAttribute("data-text") || blockquote.textContent || "";

          // remove line breaks and extra spaces
          fullText = fullText.replace(/\s+/g, " ").trim();

          blockquote.setAttribute("data-text", fullText);
          blockquote.textContent = "";

          let index = 0;
          const speed = 55;

          typingInterval = setInterval(() => {
            blockquote.textContent += fullText.charAt(index);
            index++;

            if (index >= fullText.length) {
              clearInterval(typingInterval);
            }
          }, speed);
        }

        function showSlide(n) {
          if (!slides || slides.length === 0) return;

          if (slides[current]) {
            slides[current].classList.remove('active');
          }

          current = (n + slides.length) % slides.length;

          if (slides[current]) {
            slides[current].classList.add('active');
            typeText(slides[current]);
          }
        }

        function startAuto() {
          if (!slides || slides.length === 0) return;
          autoTimer = setInterval(() => showSlide(current + 1), 8000);
        }

        const testiPrev = document.getElementById('testiPrev');
        if (testiPrev) {
          testiPrev.addEventListener('click', () => {
            clearInterval(autoTimer);
            showSlide(current - 1);
            startAuto();
          });
        }

        const testiNext = document.getElementById('testiNext');
        if (testiNext) {
          testiNext.addEventListener('click', () => {
            clearInterval(autoTimer);
            showSlide(current + 1);
            startAuto();
          });
        }

        if (slides && slides.length > 0) {
          typeText(slides[current]);
          startAuto();
        }

        // ── Featured video play ──
        const playBtn = document.getElementById('testiPlayBtn');
        const thumb = document.getElementById('testiThumb');
        const video = document.getElementById('testiVideo');

        if (playBtn && thumb && video) {
          playBtn.addEventListener('click', () => {
            thumb.style.display = 'none';
            video.style.display = 'block';
            video.play();
          });
        }

        // Technology Stack tab switching
        const techTabs = document.querySelectorAll('.tech-tab');
        const techPanels = document.querySelectorAll('.tech-icons-panel');

        const tabMap = {
          'ai-models': 'tab-ai-models',
          frameworks: 'tab-frameworks',
          vectordb: 'tab-vectordb',
          cloud: 'tab-cloud',
          mlops: 'tab-mlops'
        };

        if (techTabs && techTabs.length > 0) {
          techTabs.forEach(btn => {
            btn.addEventListener('click', () => {
              techTabs.forEach(t => {
                t.classList.remove('active');
                t.setAttribute('aria-selected', 'false');
              });

              techPanels.forEach(p => p.classList.remove('active'));

              btn.classList.add('active');
              btn.setAttribute('aria-selected', 'true');

              const panelId = tabMap[btn.dataset.tab];
              const panel = document.getElementById(panelId);

              if (panel) {
                panel.classList.add('active');
              }
            });
          });
        }
    </script>
    <script>
  const cookieBanner = document.getElementById("cookieBanner");
  const acceptBtn = document.getElementById("cookieAccept");
  const declineBtn = document.getElementById("cookieDecline");

  // Check if user already accepted
  if (!localStorage.getItem("cookieConsent")) {
    cookieBanner.style.display = "block";
  }

  acceptBtn.addEventListener("click", () => {
    localStorage.setItem("cookieConsent", "accepted");
    cookieBanner.style.display = "none";
  });

  declineBtn.addEventListener("click", () => {
    localStorage.setItem("cookieConsent", "declined");
    cookieBanner.style.display = "none";
  });
</script>
</body>

</html>
